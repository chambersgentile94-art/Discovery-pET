name: mobile
description: "Discovery-pET mobile app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_dotenv: ^6.0.1
  image_picker: ^1.2.2
  geolocator: ^14.0.2
  supabase_flutter: ^2.12.4
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  firebase_core: ^4.0.0
  firebase_messaging: ^16.0.0
  package_info_plus: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
